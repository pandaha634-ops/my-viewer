<?xml version="1.0" encoding="utf-8"?>
<!-- Layout used by the "Open with..." picker. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/titleText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:textSize="16sp"
        android:textStyle="bold" />

    <!-- The list of installed apps that can open this file.
         We give it a fixed maxHeight of 320dp so:
           - all rows are always rendered (no 0dp-height gotchas)
           - long lists scroll inside the ListView, not the dialog -->
    <ListView
        android:id="@+id/appList"
        android:layout_width="match_parent"
        android:layout_height="320dp"
        android:scrollbars="vertical" />

    <CheckBox
        android:id="@+id/alwaysUseCheckbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/always_use_this_app" />

</LinearLayout>
